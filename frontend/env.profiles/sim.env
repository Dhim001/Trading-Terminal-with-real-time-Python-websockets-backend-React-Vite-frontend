# Vite dev server for the sim terminal profile (backend HTTP 8766, WS 8765).
# Used by scripts/start-frontend.ps1 -Profile Sim

VITE_DEV_PORT=5173
VITE_BACKEND_HTTP_PORT=8766
VITE_BACKEND_WS_PORT=8765
VITE_TERMINAL_PROFILE=sim
# Empty = use same-origin /ws proxy in dev
VITE_WS_URL=
