# WebSockets server and client streams
websockets>=12.0
msgpack>=1.0.8

# HTTP REST API (Phase 3) — Starlette + uvicorn alongside WebSocket
starlette>=0.46.0
uvicorn>=0.34.0
httpx>=0.28.0

# HTTP requests for REST APIs (Alpaca/Binance integrations)
requests>=2.31.0

# Environment variable configuration
python-dotenv>=1.0.1

# Data analysis & technical indicators for trading screener bots
pandas>=2.2.0
pandas-ta-openbb>=0.4.24

# Real-time data & statistics modeling for simulations
yfinance>=1.4.1
# Google News RSS headlines (altdata news provider)
gnews>=0.5.0
arch>=8.0.0
# Parquet I/O — SBBS seed cache (data/*.parquet) and optional archive export
pyarrow>=18.0.0
# Optional scale-out (Phase 6): Redis pub/sub + PostgreSQL
redis>=5.0.0
psycopg[binary,pool]>=3.1.0
# Optional: versioned migrations (future revisions after 001_baseline)
alembic>=1.14.0
sqlalchemy>=2.0.0
# Encrypted notification channel secrets at rest
cryptography>=42.0.0
# Web Push (browser notifications)
pywebpush>=2.0.0
# Interactive Brokers market data (LIVE_IB feed-only)
ib_async>=2.0.0
# Meta-label classifier (HistGradientBoosting)
scikit-learn>=1.3.0
# Bayesian parameter search (optimizer Tier 2)
# Bayesian parameter search (optimizer Tier 2)
optuna>=3.6.0
# Background task queues
rq>=1.16.0
